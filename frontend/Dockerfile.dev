# Frontend Dockerfile for Development (Hot Reload)
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package.json yarn.lock ./
RUN npm install --frozen-lockfile

# Copy source
COPY . .

# Expose dev server port
EXPOSE 3000

CMD ["npm", "start"]
